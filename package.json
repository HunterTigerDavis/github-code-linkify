{
  "name": "github-code-linkify",
  "version": "0.7.2",
  "description": "A Chrome extension that enhances GitHub browsing by providing URL highlighting and click functionality directly on code lines in pull requests, commits, and code views. Click on any link to open it in a new tab, and view your history in the popup!",
  "author": "Hunter Tiger Davis",
  "main": "background.js",
  "type": "module",
  "scripts": {
    "sync": "node sync-version.js && git add manifest.json package.json icons/version-badge.svg",
    "patch": "pnpm version patch --no-git-tag-version && node sync-version.js --commit",
    "minor": "pnpm version minor --no-git-tag-version && node sync-version.js --commit",
    "major": "pnpm version major --no-git-tag-version && node sync-version.js --commit"
  },
  "keywords": [
    "browser-extension",
    "chrome-extension",
    "github",
    "linkify",
    "code-links",
    "pull-requests",
    "commits",
    "code-blobs",
    "url-highlighting",
    "clickable-links",
    "developer-tools",
    "productivity",
    "open-source",
    "javascript",
    "web-extension"
  ],
  "license": "MIT",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.3.0",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "badge-maker": "^5.0.2"
  }
}